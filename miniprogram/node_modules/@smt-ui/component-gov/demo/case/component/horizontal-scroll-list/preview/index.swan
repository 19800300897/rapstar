 <view class="demo swan-security-padding-bottom">
    <view class="title">不含总计栏</view>
    <gov-horizontal-scroll-list
        list-detail="{{detail}}">
    </gov-horizontal-scroll-list>
    <view class="title">含总计栏</view>
    <gov-horizontal-scroll-list
        last-row-dif="{{true}}"
        last-table-bg="#f5f5f5"
        list-detail="{{detail}}">
    </gov-horizontal-scroll-list>
    <view class="title">四字</view>
    <gov-horizontal-scroll-list
        list-detail="{{detail1}}">
    </gov-horizontal-scroll-list>
    <view class="title">左侧标题六字折断</view>
    <gov-horizontal-scroll-list
        scroll-area-width="270px"
        height-flag="{{heightFlag3}}"
        width-flag="{{widthFlag2}}"
        list-detail="{{detail3}}">
    </gov-horizontal-scroll-list>
</view>