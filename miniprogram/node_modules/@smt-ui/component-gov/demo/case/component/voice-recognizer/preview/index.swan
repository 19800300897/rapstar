<view>
    <view class="preview-caption">长语音模式（此模式不限制收音时长，适用于长字段输入）：{{voiceCont}}</view>
    <view class="preview-section" bind:tap="openVoicePanel">
        点击说话
        <gov-voice-recognizer
            show-panel="{= showPanel =}"
            bindstop="getCont"
            longSpeech="{{true}}"
            bindcancelend="cancelEnd"
            hasTabBar="{{false}}"
        />
    </view>
    <view class="preview-caption">短语音模式（此模式支持60秒以内识别，适用于短字段输入）：{{voiceShortCont}}</view>
    <view class="preview-section" bind:tap="openShortVoicePanel">
        点击说话
        <gov-voice-recognizer
            show-panel="{= showShortPanel =}"
            bindstop="getShortCont"
            bindcancelend="cancelShortEnd"
        />
    </view>
    <view class="preview-caption">听音模式（dnn，此模式自动听音,默认值）：{{voiceAutoCont}}</view>
    <view class="preview-section" bind:tap="openAutoVoicePanel">
        点击说话
        <gov-voice-recognizer
            show-panel="{= showAutoPanel =}"
            bindstop="getAutoCont"
            bindcancelend="cancelAutoEnd"
            mode="dnn"
        />
    </view>
    <view class="preview-caption">听音模式（touch，此模式手动听音，需要手动调用stop方法才会停止语音识别）：{{voiceManualCont}}</view>
    <view class="preview-section" bind:tap="openManualVoicePanel">
        点击说话
        <gov-voice-recognizer
            show-panel="{= showManualPanel =}"
            bindstop="getManualCont"
            bindcancelend="cancelManualEnd"
            mode="touch"
        />
    </view>
    <view class="preview-caption">自定义语音文案：{{voiceTextCont}}</view>
    <view class="preview-section" bind:tap="openTextVoicePanel">
        点击说话
        <gov-voice-recognizer
            show-panel="{= showTextPanel =}"
            bindcancelend="cancelTextEnd"
            bindstop="getTextCont"
            defaultText="聆听中，请说话..."
        />
    </view>
    <view class="preview-caption">自定义样式：{{voiceColorCont}}</view>
    <view class="preview-section" bind:tap="openColorVoicePanel">
        点击说话
        <gov-voice-recognizer
            show-panel="{= showColorPanel =}"
            bindcancelend="cancelColorEnd"
            bindstop="getColorCont"
            gov-mark="gov-mark"
            gov-btn-voice="gov-btn-voice"
            gov-voice-panel="gov-voice-panel"
            gov-word-box="gov-word-box"
        />
    </view>
</view>