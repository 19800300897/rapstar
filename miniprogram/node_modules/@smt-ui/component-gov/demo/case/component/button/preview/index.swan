<view class="button-text">
    小按钮:
    <gov-button
        button-size="small"
        button-color="default"
        button-text="最多四字">
    </gov-button>
    中按钮:
    <gov-button
        button-size="middle"
        button-color="default"
        button-text="文案最多六字">
    </gov-button>
    大按钮:
    <gov-button
        button-size="large"
        button-color="default"
        button-text="按钮文案最多八字">
    </gov-button>
    可点击的按钮:
    <gov-button
        button-size="large"
        button-color="default"
        button-text="按钮文案最多八字"
        button-disabled="{{false}}">
    </gov-button>
    不可点击的按钮:
    <gov-button
        button-size="large"
        button-color="default"
        button-text="不可点击的按钮"
        button-disabled="{{true}}">
    </gov-button>
    字数很多的按钮:
    <gov-button
        button-size="large"
        button-color="default"
        button-text="字数很多的按钮字数很多的按钮字数很多的按钮字数很多的按钮字数很多的按钮"
        button-disabled="{{false}}">
    </gov-button>
    带icon的按钮:
    <gov-button
        button-size="large"
        button-color="default"
        button-text="带icon的按钮"
        button-icon="../../../../src/calendar/images/arrow-right.png"
        button-disabled="{{false}}">
    </gov-button>
    自定义样式的按钮：
    <gov-button
        button-size="large"
        button-color="default"
        button-text="自定义样式的按钮"
        gov-button="textColor"
        bind:click="clickFn"
        button-disabled="{{false}}">
    </gov-button>
</view>