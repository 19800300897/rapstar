"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getMonth = exports.getDay = void 0;

/**
 * @file util.js
 * @author huangzilong (huangzilong@baidu.com)
 */

/**
 * 获取当前日期，格式 yyyy-mm-dd
 *
 * @param {Number} offset 偏移，+- number
 * @param {Date} d 当前日期
 * @param {string} separation 分隔
 */
var getDay = function getDay() {
  var offset = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
  var d = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : new Date();
  var separation = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : '-';
  var day = new Date(d.getFullYear(), d.getMonth(), d.getDate() + offset);
  var month = day.getMonth() + 1;
  var today = day.getDate();
  return [day.getFullYear(), month > 9 && month || '0' + month, today > 9 && today || '0' + today].join(separation);
};
/**
 * 获取当前年月 yyyy-mm
 *
 * @param {Number} offset 月份偏移 +- number
 * @param {Date} obj 当前日期
 * @param {string} separation 分隔
 */


exports.getDay = getDay;

var getMonth = function getMonth() {
  var offset = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
  var d = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : new Date();
  var separation = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : '-';
  var m = new Date(d.getFullYear(), d.getMonth() + offset);
  var month = m.getMonth() + 1;
  return [m.getFullYear(), month > 9 && month || '0' + month].join(separation);
};

exports.getMonth = getMonth;