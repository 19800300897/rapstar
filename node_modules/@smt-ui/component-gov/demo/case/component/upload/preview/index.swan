<view class="upload-box">
    <view class="box">
        普通上传/code为0/上传成功
    </view>
    <gov-upload
        count="6"
        bind:uploadsuccess="tap"
        bind:delete="clickDelete"
        limitSize="{{1}}"
        tips="最多支持6张图片"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
        imageList="{{imageList}}"
    />
    <view class="box">
        不传url。暂时取值为选择图片本地路径
    </view>
    <gov-upload
        count="6"
        bind:urlempty="urlempty"
        bind:delete="deleteUrlempty"
        limitSize="{{1}}"
        tips="最多支持6张图片"
        imageList="{{urlemptyImage}}"
        bind:previewfail="previewfail"
    />
    <view class="box">
        普通上传/code不为0/上传失败
    </view>
    <gov-upload
       count="6"
        bind:uploadsuccess="taps"
        tips="最多支持6张图片"
        limitSize="{{1}}"
        bind:delete="clickDeletes"
        imageList="{{showImage}}"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
    />
    <view class="box">
        普通上传/code不为0/图片太大或格式不符合
    </view>
    <gov-upload
       count="6"
        bind:uploadsuccess="tapImgSize"
        tips="最多支持6张图片"
        limitSize="{{1}}"
        bind:delete="sizeDelete"
        imageList="{{sizeImage}}"
        bind:againup="againup"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
    />
    <view class="box">
        粗分割
    </view>
    <gov-upload
        count="6"
        bind:uploadsuccess="borderTap"
        bind:delete="borderDelete"
        tips="最多支持6张图片"
        limitSize="{{1}}"
        imgMode="scaleToFill"
        sizeType="{{['compressed']}}"
        imageList="{{borderList}}"
        border="{{true}}"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
    />
    <view class="box">
        externalClasses
    </view>
    <gov-upload
        count="6"
        gov-tips="gov-tips"
        tips="最多支持6张图片"
        limitSize="{{1}}"
        bind:uploadsuccess="classUp"
        bind:delete="classDelete"
        imageList="{{classList}}"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
    />
    <view class="box">
        不保持纵横比缩放图片，使图片的宽高完全拉伸至填满 image 元素
    </view>
    <gov-upload
        count="1"
        gov-tips="gov-tips"
        tips="最多支持1张图片"
        bind:uploadsuccess="lineUp"
        bind:delete="lineDelete"
        imageList="{{lineList}}"
        limitSize="{{1}}"
        inline="{{true}}"
        imgMode="scaleToFill"
        border="{{true}}"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
    />
    <view class="box">
        保持纵横比缩放图片，使图片的长边能完全显示出来。也就是说，可以完整地将图片显示出来。
    </view>
    <gov-upload
        count="1"
        gov-tips="gov-tips"
        tips="最多支持1张图片"
        bind:uploadsuccess="lineUp"
        bind:delete="lineDelete"
        imageList="{{lineList}}"
        inline="{{true}}"
        limitSize="{{1}}"
        imgMode="aspectFit"
        border="{{true}}"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
    />
    <view class="box">
        保持纵横比缩放图片，只保证图片的短边能完全显示出来。也就是说，图片通常只在水平或垂直方向是完整的，另一个方向将会发生截取。
    </view>
    <gov-upload
        count="1"
        gov-tips="gov-tips"
        tips="最多支持1张图片"
        bind:uploadsuccess="lineUp"
        bind:delete="lineDelete"
        imageList="{{lineList}}"
        inline="{{true}}"
        limitSize="{{1}}"
        imgMode="aspectFill"
        border="{{true}}"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
    />
    <view class="box">
        宽度不变，高度自动变化，保持原图宽高比不变
    </view>
    <gov-upload
        count="1"
        gov-tips="gov-tips"
        tips="最多支持1张图片"
        bind:uploadsuccess="lineUp"
        bind:delete="lineDelete"
        imageList="{{lineList}}"
        inline="{{true}}"
        limitSize="{{1}}"
        imgMode="widthFix"
        border="{{true}}"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
    />
    <view class="box">
        externalClasses改变图片盒子的高度
    </view>
    <gov-upload
        count="1"
        gov-tips="gov-tips"
        tips="最多支持1张图片"
        bind:uploadsuccess="lineUp"
        bind:delete="lineDelete"
        gov-upload-inline-box="gov-upload-inline-box"
        imageList="{{lineList}}"
        inline="{{true}}"
        limitSize="{{1}}"
        imgMode="scaleToFill"
        border="{{true}}"
        url="https://smartprogram.baidu.com/mappconsole/api/checkFile"
    />
</view>